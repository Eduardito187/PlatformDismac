import * as React from 'react';
import { View } from 'react-native';
import { Space_Separation } from '../Modules/Login/Style/style';

const Space = (props) => {
    React.useEffect(() => {
        //
    }, []);

    return (
        <View style={Space_Separation}></View>
    );
}

export default Space;